
  # Song Similarity Finder

  This is a code bundle for Song Similarity Finder. The original project is available at https://www.figma.com/design/5PsJVhR1hyDL6NzocUYJgY/Song-Similarity-Finder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  